name: "Jellyfish Bridge"
guid: "93b3913f-bb53-4120-94a5-1b10e4280f50"
version: "0.2.0.1"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Bridge between Jellyfin and Jellyseerr / Radarr / Sonarr for the Jellyfish mobile app."
description: >
  Exposes Jellyseerr, Radarr and Sonarr behind Jellyfin authentication so the
  Jellyfish mobile client only needs a single Jellyfin connection. Jellyseerr
  calls are attributed to the matching Jellyfin user via jellyfinUserId mapping.
category: "General"
owner: "Jellyfish-Client"
imageUrl: "https://raw.githubusercontent.com/Jellyfish-Client/plugin/main/Jellyfish.Bridge/logo.png"
artifacts:
  - "Jellyfish.Bridge.dll"
changelog: |-
  ### 0.2.0.1
  Revert the 0.2.0.0 web UI takeover — the injected `/jellyfish/ui/home.js` script triggered a browser HTTP Basic auth prompt on the Jellyfin login page and locked users out of the web client. This release returns to the 0.1.0.6 bridge-only behaviour.

  ### 0.1.0.6
  Add Jellyseerr `movie/{id}/similar` and `tv/{id}/similar` passthroughs so the "Because you watched…" rail can resolve.

  ### 0.1.0.5
  Forward full query string on Jellyseerr discover passthroughs.

  ### 0.1.0.4
  Expose `GET /jellyfish/jellyseerr/config` so the mobile client can hit Jellyseerr directly for advanced discover queries.

  ### 0.1.0.3
  Add Jellyseerr proxy endpoints (watchproviders, genreslider, discover/watchlist) and fix the CI changelog-injection step.

  ### 0.1.0.1
  Add plugin logo.

  ### 0.1.0.0
  Initial scaffold.
